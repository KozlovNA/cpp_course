--------------------------------------------------------------------------------
-> 4.15
wl: c++ evaluator
динамическое выделение памяти на этапе компиляции, значит и освобождение должно происходить в compile time 

std::cout << std::hex << x;
std:cout << std::format("main : x = {:#x}\n", x);
std::print("main : x = {:#x}\n", x);
std::println("main : x = {:#x}", x);
